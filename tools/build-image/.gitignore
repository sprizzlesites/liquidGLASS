# Transient working directories created by build.sh at build time.
# Never committed: _context/ is a staging copy of tools/skel/ for the docker
# build context, and _v86-ref/ is a throwaway `git clone --depth 1` of
# copy/v86 used only to borrow tools/fs2json.py + tools/copy-to-sha256.py.
_context/
_v86-ref/
